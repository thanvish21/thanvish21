name: Repository Baseline

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  repository-baseline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify repository essentials
        run: |
          test -f README.md
          test -f LICENSE
